# MediaGovernor 界面合同

### UI decision: 真实样本识别实验

- 目标与主路径：用户从现有问题卡片进入详情，选择 2～5 个已经人工理解的真实案例，对同一份脱敏证据比较完整树、压缩卡、压缩卡加现状三种输入；结果只显示识别、模型、耗时和 usage。
- 信息层级：真实问题和等待确认仍是主页面主体；实验区单独置于结论区之前，并明确它不会整理、删除或重建文件。
- 数据与权限：只提交地图内部短 ID；后端从私有地图生成无路径、无正确答案的证据。实验端点只读、需 bearer 鉴权，最多 5 个作品、3 种输入、单项串行。
- 关键状态：未选样本、少于 2 项、运行中、完整结果、超时/失败和供应商未返回 token 都有独立文案；失败时保留已完成批次回执。
- 现有基础：沿用 Vue 3、现有按钮、卡片、颜色和响应式断点；不新增组件库、账号、MCP、外部资产或动效。
- a11y 与性能：使用原生 button、heading 和文本状态；实验不自动启动，首轮固定 batch size 1，最长请求由后端 90 秒硬上限和页面总预算共同约束。
- 验收：Python API 合同测试、Vue 构建、前端字符串合同、真实 MoviePilot 页面能选两个案例并显示三种输入的逐次回执。
- 回滚：移除两个实验 API、实验区状态和样本按钮即可；媒体地图与修复链不迁移、不改写。

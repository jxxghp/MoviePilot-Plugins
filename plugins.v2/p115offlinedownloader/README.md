# 115离线下载器 0.1.0

请先安装并启用 P115StrmHelper，再在 MoviePilot 中创建类型为 `p115offline` 的非默认自定义下载器。
插件支持原生 BTIH 磁力和公开 v1 Torrent；私有 Torrent、敏感 Tracker 与纯 v2 Torrent 会被强制拒绝。

连接测试使用已保存的插件配置，不会提交真实离线任务。完整安装、配置及验收说明见仓库根目录 README。
